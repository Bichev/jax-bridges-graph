# ============================================
# JAX Business Relationship Mapper
# Environment Configuration
# ============================================

# --------------------------------------------
# REQUIRED: OpenAI API Configuration
# --------------------------------------------

# Your OpenAI API key (REQUIRED)
# Get one at: https://platform.openai.com/api-keys
# Format: sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_API_KEY=your_openai_api_key_here


# --------------------------------------------
# OPTIONAL: OpenAI Model Settings
# --------------------------------------------

# AI Model to use for analysis
# Options: gpt-4o, gpt-4-turbo, gpt-4, gpt-3.5-turbo
# Default: gpt-4o (recommended for best results)
# Cost: gpt-4o is ~$0.03 per relationship analyzed
OPENAI_MODEL=gpt-4o

# Temperature setting (creativity vs consistency)
# Range: 0.0 to 2.0
# Lower (0.5-0.7): More consistent, focused analysis
# Higher (0.8-1.2): More creative, diverse relationships
# Default: 0.7 (balanced)
OPENAI_TEMPERATURE=0.7

# Maximum tokens per API response
# Range: 500 to 4000
# Higher = more detailed analysis, but costs more
# Default: 1000 (good balance)
OPENAI_MAX_TOKENS=1000


# --------------------------------------------
# OPTIONAL: Retry & Rate Limiting
# --------------------------------------------

# Maximum number of retry attempts on API failures
# Default: 3
MAX_RETRIES=3

# Initial delay between retries (milliseconds)
# Increases exponentially with each retry
# Default: 1000 (1 second)
RETRY_DELAY=1000

# Delay between API requests to avoid rate limits (milliseconds)
# Default: 500 (0.5 seconds)
# Increase if you hit rate limits frequently
RATE_LIMIT_DELAY=500


# --------------------------------------------
# OPTIONAL: Analysis Configuration
# --------------------------------------------

# Minimum confidence score to include relationships
# Range: 0 to 100
# Default: 50 (only include moderately confident matches)
MIN_CONFIDENCE_THRESHOLD=50

# Enable debug logging for analysis script
# Options: true, false
# Default: false
DEBUG_MODE=false


# --------------------------------------------
# NOTES
# --------------------------------------------

# 1. DO NOT commit this file with real values to Git!
#    Only commit .env.example with placeholder values.
#
# 2. Create your actual .env file:
#    cp .env.example .env
#    Then edit .env with your real values.
#
# 3. Cost Estimates (using gpt-4o):
#    - Sample analysis (5 businesses): ~$0.05-0.10
#    - Full analysis (27 businesses): ~$0.50-1.00
#
# 4. Rate Limits (OpenAI free tier):
#    - 3 requests per minute
#    - 200 requests per day
#    - The script handles this automatically!
#
# 5. For production or heavy use:
#    - Consider OpenAI paid tier
#    - Adjust RATE_LIMIT_DELAY if needed
#    - Monitor costs at: https://platform.openai.com/usage

# ============================================
# Quick Setup Commands
# ============================================
#
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Edit .env and add your real OpenAI key
#
# 3. Test with sample data:
#    npm run analyze:sample
#
# 4. Run full analysis:
#    npm run analyze
#
# ============================================
